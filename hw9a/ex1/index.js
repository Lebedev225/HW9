const circles =  require("./circles.js")

const radius = 3;


console.log(`The circumference of a circle with radius ${radius} is ${circles.circumference(radius)}`);
console.log(`Its area is ${circles.area(radius)}`);